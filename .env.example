# Environment variables for WorkBox
# Copy this file to .env and update with your actual MySQL credentials

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_actual_mysql_password
DB_NAME=workbox_db

# Optional: Full database URL (alternative to individual settings)
# DATABASE_URL=mysql+pymysql://root:your_password@localhost/workbox_db
